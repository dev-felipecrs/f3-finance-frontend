import { SelectRoot } from './Root'
import { SelectLabel } from './Label'
import { SelectField } from './Field'
import { SelectErrorMessage } from './ErrorMessage'

export const Select = {
  ErrorMessage: SelectErrorMessage,
  Field: SelectField,
  Label: SelectLabel,
  Root: SelectRoot,
}
